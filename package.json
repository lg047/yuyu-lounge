{
  "name": "yuyu-lounge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "serve:static": "browser-sync start --server --files 'index.html, about.html, songs.html, css/**/*.css, js/**/*.js, assets/**/*'"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "browser-sync": "^2.29.3"
  }
}
