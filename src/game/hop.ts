import type { Core } from "./core/loop";

const game = {
  meta: { id: "hop", title: "Cloud Hop", bestKey: "best.hop" },
  init(_canvas: HTMLCanvasElement, _core: Core) {},
  start() {},
  stop() {},
  destroy() {}
};

export default game;
