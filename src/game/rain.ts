import type { Core } from "./core/loop";

const game = {
  meta: { id: "rain", title: "Treat Rain", bestKey: "best.rain" },
  init(_canvas: HTMLCanvasElement, _core: Core) {},
  start() {},
  stop() {},
  destroy() {}
};

export default game;
